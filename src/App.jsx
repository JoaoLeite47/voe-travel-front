import { BrowserRouter } from "react-router-dom";
import RoutesPath from "./routes/routesPath";

function App() {
  return (
    <BrowserRouter>
      <RoutesPath />
    </BrowserRouter>
  );
}

export default App;
